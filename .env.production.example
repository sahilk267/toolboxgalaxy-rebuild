# Build this static Vite app with the same-origin PHP endpoint configured for Hostinger.
VITE_CONTACT_ENDPOINT=/api/v1/contact

# Optional: use this only after exporting the supplied original artwork to public_html/assets/.
VITE_ASSET_BASE_URL=/assets
